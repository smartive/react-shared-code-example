import people from './people';

/**
 * Exports a map of all reducers. In a real project this map would obviously be larger, but is
 * alright for example purposes.
 *
 * @export
 * @default
 */
export default {
  people,
};
